#include "monty.h"
/**
  *f_nop- nothing.
  *@head:it has stack head.
  *@counter:it is line_number.
  *Return:it has no return.
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
